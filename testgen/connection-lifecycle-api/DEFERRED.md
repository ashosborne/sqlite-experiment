# Deferred — testgen/connection-lifecycle-api (run 10)

Operator DO_NOT_RECORD list:
- URI matrix (mode=/vfs=/cache=): card notes SQLITE_USE_URI defaults off; the pinned fingerprint does
  not list USE_URI — parked until an SME scopes URI behaviour on this pin.
- Empty-filename temp-file open.
- open16 / UTF-16 variants.
- Illegal open_v2 flag combinations (MISUSE) — needs a flag-matrix SME first.
- busy handler / hooks / trace (features 003/004 — other IDs, other runs).
