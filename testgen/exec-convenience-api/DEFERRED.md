# Deferred — testgen/exec-convenience-api (run 10)
Operator scope: no multi-statement rollback semantics, no CREATE TABLE side-effect cases, no
abort-mid-script (multi-statement) case this run. errmsg-ownership corpus later. get_table
(exec-convenience-api-002) is another ID.
