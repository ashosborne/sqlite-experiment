# SME brief — window-functions (Phase A)
**Found:** 2 candidates. Frame semantics (002) is the risk centre.
**Recommended binds:** accept both if downstream SQL uses OVER; else defer (prose).
STOPPED for human bind. No Phase B performed.
