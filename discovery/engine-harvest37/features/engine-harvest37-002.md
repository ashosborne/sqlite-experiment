# engine-harvest37-002 — authorizer argument strings + IGNORE

Confidence: observed-in-code (composed slice, run 47).
probed s1-s4 for INSERT/UPDATE-per-column/DELETE/SELECT+ordered READs/PRAGMA/TRANSACTION/ATTACH/DETACH; IGNORE skips INSERT/UPDATE, DELETE proceeds.
Frozen scope = run-47 pinned cases (pack v37).
