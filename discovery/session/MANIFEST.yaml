slice_id: session
seed: "session extension: change tracking, changesets/patchsets, apply/conflict"
seed_entrypoints: ["sqlite3session_create()", "sqlite3changeset_apply()"]
out_of_scope_hints: ["changeset CLI tools", "fuzz harness"]
phase: A
updated_at: 2026-08-10T18:09:13Z
features:
  - id: session-001
    name: Session lifecycle and change recording
    status: candidate
    confidence: observed-in-code
    summary: "Create/attach/generate-changeset over tracked tables; indirect-change flags."
    entrypoints:
      - kind: api
        locator: "sqlite3session_create()"
        evidence: ["ext/session/sqlite3session.c:2362", "ext/session/sqlite3session.c:2473", "ext/session/sqlite3session.c:3213"]
    dependencies: ["connection-lifecycle-api"]
    open_questions: []
  - id: session-002
    name: Changeset apply with conflict resolution
    status: candidate
    confidence: observed-in-code
    summary: "Apply with per-conflict callback returning OMIT/REPLACE/ABORT; foreign-key ordering rules."
    entrypoints:
      - kind: api
        locator: "sqlite3changeset_apply()"
        evidence: ["ext/session/sqlite3session.c:5940"]
    dependencies: ["session-001"]
    open_questions: []
  - id: session-003
    name: Changeset algebra (iterate / invert / concat)
    status: candidate
    confidence: observed-in-code
    summary: "Iterator API over binary changeset format; invert and concat composition; patchset subset."
    entrypoints:
      - kind: api
        locator: "sqlite3changeset_start()"
        evidence: ["ext/session/sqlite3session.c:3407", "ext/session/sqlite3session.c:4308", "ext/session/sqlite3session.c:6814"]
    dependencies: ["session-001"]
    open_questions: ["Binary changeset format stability is the migration contract"]
