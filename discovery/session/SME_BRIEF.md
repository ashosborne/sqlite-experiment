# SME brief — session (Phase A)
**Found:** 3 candidates. Binary changeset format = the real interchange contract; flag for format-level characterization.
**Recommended binds:** accept all if downstream sync uses sessions.
STOPPED for human bind. No Phase B performed.
