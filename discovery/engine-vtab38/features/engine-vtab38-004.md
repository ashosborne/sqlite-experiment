# engine-vtab38-004 — xUpdate writable vtabs

Confidence: observed-in-code (composed slice, run 48).
INSERT auto/explicit rowid (argv[0..1] shapes, last_insert_rowid), UPDATE old/new rowid, DELETE argc=1, constraint rc 19, argv-shape log pinned; module storage drives scans.
Frozen scope = run-48 pinned cases (pack v38).
