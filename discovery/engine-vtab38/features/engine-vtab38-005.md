# engine-vtab38-005 — eponymous-only modules

Confidence: observed-in-code (composed slice, run 48).
xCreate NULL: bare-name SELECT connects; CREATE VIRTUAL TABLE refuses no such module; no schema row.
Frozen scope = run-48 pinned cases (pack v38).
