# SME brief — wasm-opfs (Phase A, resume run 2)
**Found:** 2 candidates (the two OPFS strategies). Deployment choice determines concurrency + header requirements.
**Recommended binds:** with the browser-scope decision; pick one variant to characterize first.
STOPPED for human bind. No Phase B performed.
