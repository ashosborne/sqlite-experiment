# icu-001 — ICU function and collation pack

Slice: `icu` · Status: `documented` · Confidence: `observed-in-code` · Card written: 2026-08-11T10:32:22Z
As-is behaviour card (Discovery v0.2 Phase B). Not a redesign, not a user story.

## Summary

ICU-backed like/regexp/upper/lower overrides + icu_load_collation for locale collations.

## Entrypoints (citations)

- `sqlite3IcuInit()` (other) — `ext/icu/icu.c:538`, `ext/icu/icu.c:546`, `ext/icu/icu.c:213`, `ext/icu/icu.c:465`

## Inputs / outputs / observables

- ICU-backed upper()/lower() with locale arg; LIKE case-insensitivity beyond ASCII; regexp() via ICU uregex; icu_load_collation('locale','name') then COLLATE name

## Behaviour (as implemented)

- sqlite3IcuInit (ext/icu/icu.c:538) registers the scalar table (:546): icuLikeFunc (:213) full-unicode case folding, icuRegexpFunc (:280) ICU regex dialect, locale-aware upper/lower overriding built-ins, icuLoadCollation (:465) creates UCollator-backed collations at runtime

## Validation rules found in code

- Bad locale/regex → SQL error with ICU status text

## Edge cases found in code

- Overrides the built-in like() — changes LIKE-optimizer eligibility (case_sensitive_like interplay)
- Collation order varies by ICU library version — environment-parity risk (run-2 flag)

## Dependencies

- builtin-scalar-agg-funcs

## Assumptions / unknowns

- Not part of default baseline build (requires ICU linkage)
- ICU library version pinning affects collation order — parity risk

## Evidence

- `ext/icu/icu.c:538`
- `ext/icu/icu.c:546`
- `ext/icu/icu.c:213`
- `ext/icu/icu.c:465`
