# SME brief — wasm-js-api (Phase A, resume run 2)
**Found:** 3 candidates layering the JS surface run-1's umbrella only pointed at. worker1 (003) is an async message protocol — flagged for card splitting.
**Recommended binds:** tie to the browser-scope decision; if in scope, 002 (oo1) first.
STOPPED for human bind. No Phase B performed.
