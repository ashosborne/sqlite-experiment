# SME brief — serialize-memdb-api (Phase A)
**Found:** 2 candidates (public round-trip API + backing VFS). Compile-flag gated — verify target build enables it.
**Recommended binds:** accept 001; 002 accept if in-memory shared dbs are used downstream.
STOPPED for human bind. No Phase B performed.
