# engine-lookaside35-003 — status coupling + regression

Confidence: observed-in-code (composed slice, run 45).
DEFAULT pool serves prepare traffic; MEMORY_USED sane + SCHEMA_USED positive hi 0 alongside the pool; default-config light traffic keeps MISS_SIZE/FULL exact zeros; bad db_status op still ERROR.
Frozen scope = run-45 pinned cases (pack v35).
