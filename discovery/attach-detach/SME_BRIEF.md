# SME brief — attach-detach (Phase A)
**Found:** 3 candidates. SQL-surface seam, characterizable via PRAGMA database_list + error codes.
**Recommended binds:** accept 001+002; 003 is subtle DDL policy — accept if attached-db DDL is used downstream.
STOPPED for human bind. No Phase B performed.
