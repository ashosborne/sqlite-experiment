# CANDIDATES — misc-regexp (Phase A, unbound; resume run 2)

All `candidate`, confidence `observed-in-code`. Thin unbundle refining run-1 umbrella `misc-func-packs` (umbrella row untouched). STOP for human bind.

| # | Candidate | Evidence | Why in slice |
| --- | --- | --- | --- |
| 001 | regexp() pattern matching | `ext/misc/regexp.c:901` | NFA regex engine backing the REGEXP operator; own dialect (not PCRE) |
