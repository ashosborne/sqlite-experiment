# fts5-003 — Auxiliary functions (highlight/snippet/bm25) via fts5_api

Slice: `fts5` · Status: `documented` · Confidence: `observed-in-code` · Card written: 2026-08-11T10:30:12Z
As-is behaviour card (Discovery v0.2 Phase B). Not a redesign, not a user story.

## Summary

User-visible fts5_api object exposes aux-function registration; built-in ranking.

## Entrypoints (citations)

- `fts5_api` (other) — `ext/fts5/fts5_main.c:79`

## Inputs / outputs / observables

- highlight(tbl,col,open,close), snippet(...), bm25(tbl,weights...) results; custom aux functions via fts5_api.xCreateFunction

## Behaviour (as implemented)

- fts5_api object (ext/fts5/fts5_main.c:79) retrieved via SELECT fts5(?1) pointer-passing; aux functions run per-row with Fts5ExtensionApi access to phrase match positions; bm25 is default rank source

## Validation rules found in code

- Aux function only callable on an fts5 table's MATCH query context

## Edge cases found in code

- snippet column selection -1 auto-picks; position lists depend on detail= mode (full/column/none reduce capability)

## Dependencies

- fts5-001

## Assumptions / unknowns

- Index-format stability across versions relevant to migration?

## Evidence

- `ext/fts5/fts5_main.c:79`
