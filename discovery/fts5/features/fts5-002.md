# fts5-002 — Tokenizer API and built-in tokenizers

Slice: `fts5` · Status: `documented` · Confidence: `observed-in-code` · Card written: 2026-08-11T10:30:12Z
As-is behaviour card (Discovery v0.2 Phase B). Not a redesign, not a user story.

## Summary

xCreateTokenizer(_v2) registration; unicode61/ascii/porter/trigram built-ins.

## Entrypoints (citations)

- `fts5_api.xCreateTokenizer()` (other) — `ext/fts5/fts5_main.c:3217`, `ext/fts5/fts5_main.c:3252`, `ext/fts5/fts5_tokenize.c:556`

## Inputs / outputs / observables

- tokenize= option effects (unicode61 defaults, ascii, porter stemming wrapper, trigram substring matching); custom tokenizer registration via fts5_api

## Behaviour (as implemented)

- xCreateTokenizer(_v2) (registration path ext/fts5/fts5_main.c:3217,3252) stores tokenizer factories; v2 interface (ext/fts5/fts5_tokenize.c:556 region) adds locale support; built-ins: unicode61 (categories+removediacritics+separators args), ascii, porter (wraps another), trigram (enables LIKE/GLOB index use)

## Validation rules found in code

- Unknown tokenizer name errors at table create

## Edge cases found in code

- Tokenizer determinism is an index-consistency requirement — changing args on existing index corrupts matching (documented)

## Dependencies

- fts5-001

## Assumptions / unknowns

- (none found in code)

## Evidence

- `ext/fts5/fts5_main.c:3217`
- `ext/fts5/fts5_main.c:3252`
- `ext/fts5/fts5_tokenize.c:556`
