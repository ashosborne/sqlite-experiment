# fts5-001 — FTS5 virtual table module

Slice: `fts5` · Status: `documented` · Confidence: `observed-in-code` · Card written: 2026-08-11T10:30:12Z
As-is behaviour card (Discovery v0.2 Phase B). Not a redesign, not a user story.

## Summary

fts5 vtab create/connect, content storage variants, MATCH query execution.

## Entrypoints (citations)

- `sqlite3_fts5_init()` (other) — `ext/fts5/fts5_main.c:3891`, `ext/fts5/fts5_main.c:3763`, `ext/fts5/fts5_main.c:477`

## Inputs / outputs / observables

- CREATE VIRTUAL TABLE ... USING fts5(...); MATCH query syntax (implicit AND, OR/NOT, NEAR(), phrase "...", prefix *, column filters); rank ordering; insert/delete/'delete-all'/'rebuild' special commands

## Behaviour (as implemented)

- sqlite3_fts5_init (ext/fts5/fts5_main.c:3891) → fts5Init (:3763) registers module + fts5() pointer function; fts5CreateMethod (:477) parses column/option args (content=, contentless, external content, tokenize=, prefix=); MATCH compiles an expression tree over the inverted index; shadow tables _data/_idx/_content/_docsize/_config

## Validation rules found in code

- Bad query syntax → 'fts5: syntax error near'; contentless tables reject some operations

## Edge cases found in code

- External-content tables require user-maintained sync (or triggers); 'rebuild' command re-indexes

## Dependencies

- vtab-core

## Assumptions / unknowns

- Index-format stability question (run 1) stands for migration

## Evidence

- `ext/fts5/fts5_main.c:3891`
- `ext/fts5/fts5_main.c:3763`
- `ext/fts5/fts5_main.c:477`
