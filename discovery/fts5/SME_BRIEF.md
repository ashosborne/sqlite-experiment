# SME brief — fts5 (Phase A)
**Found:** 3 candidates for the current-generation FTS. Query-syntax + ranking parity are the risk centres.
**Recommended binds:** accept all if downstream uses FTS5; index-format question raised.
STOPPED for human bind. No Phase B performed.
