# SME brief — misc-vtab-packs (Phase A)
**Found:** 1 cluster candidate (~18 loadable vtabs). generate_series build-config question flagged (often assumed built-in).
**Recommended binds:** per-vtab accept based on downstream usage list.
STOPPED for human bind. No Phase B performed.
