# engine-compile32-001 — compileoption diagnostics C + SQL

Confidence: observed-in-code (composed slice, run 42).
used() plain/prefixed/=value/case-insensitive forms; get() enumeration with NULL past either end; SQL twins; 38-entry pinned fingerprint incl. COMPILER row (pin decision, ADR 0030).
Frozen scope = run-42 pinned cases (pack v32).
