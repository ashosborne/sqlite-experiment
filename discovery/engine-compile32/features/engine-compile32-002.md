# engine-compile32-002 — OMIT census on the pin

Confidence: observed-in-code (composed slice, run 42).
OMIT_LOAD_EXTENSION/WAL/VIRTUALTABLE/TRIGGER/ATTACH/SUBQUERY/VIEW/AUTORESET/COMPILEOPTION_DIAGS all 0; zero OMIT_ entries enumerated.
Frozen scope = run-42 pinned cases (pack v32).
