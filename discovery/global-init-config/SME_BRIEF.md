# SME brief — global-init-config (Phase A, resume run 2)
**Found:** 3 candidates closing the run-1 open question. db_config toggle inventory (003) is the highest-value bind: defaults silently shape every other slice's behaviour.
**Recommended binds:** accept all three; 003 first.
STOPPED for human bind. No Phase B performed.
