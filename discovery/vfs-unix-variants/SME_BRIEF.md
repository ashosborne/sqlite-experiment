# SME brief — vfs-unix-variants (Phase A, resume run 2)
**Found:** 3 candidates covering the paths run 1 explicitly left out. All platform-scope decisions.
**Recommended binds:** likely defer all three unless macOS/NFS/VxWorks deployments exist (prose).
STOPPED for human bind. No Phase B performed.
