# engine-harvest42-002 — recursive UNION ALL FIFO

Confidence: observed-in-code (composed slice, run 52).
C's Queue/Current machine: one extracted row is the recursive table per step; multi-seed terms interleave in FIFO order; multi-column recursion.
Frozen scope = run-52 pinned cases (pack v42).
