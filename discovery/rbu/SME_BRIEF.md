# SME brief — rbu (Phase A)
**Found:** 2 candidates. Complex multi-stage async-like workflow (state db + wal handoff) — flagged for card splitting at bind.
**Recommended binds:** accept if downstream uses OTA-style updates; else defer (prose).
STOPPED for human bind. No Phase B performed.
