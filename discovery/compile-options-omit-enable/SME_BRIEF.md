# SME brief — compile-options-omit-enable (Phase A, resume run 2)
**Found:** 3 candidates turning run-1 residual 1 into inventory. 001 is the runtime oracle; 002/003 are guard censuses (counts cited, not per-option cards — per-option carding only makes sense after the baseline build is pinned).
**Recommended binds:** accept 001 immediately (baseline pinning precedes all characterization); 002/003 as a baseline-decision work item, not per-option cards.
STOPPED for human bind. No Phase B performed.
