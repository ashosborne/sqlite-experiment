# engine-harvest41-004 — SQLITE_ALTER_TABLE outer code

Confidence: observed-in-code (composed slice, run 51).
code 26 with INVERTED args (s1 = database, s2 = table) for RENAME and ADD COLUMN; IGNORE silently no-ops; DENY rc 23.
Frozen scope = run-51 pinned cases (pack v41). No sqlite_master tails frozen.
