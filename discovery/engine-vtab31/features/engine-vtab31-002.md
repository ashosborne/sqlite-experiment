# engine-vtab31-002 — declare_vtab column shape

Confidence: observed-in-code (composed slice, run 41).
declare_vtab fixes names/types inside xCreate/xConnect (MISUSE outside); HIDDEN excluded from SELECT * but selectable; pragma table_info shape; typeof through xColumn.
Frozen scope = run-41 pinned cases (pack v31).
