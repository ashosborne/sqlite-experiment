# SME brief — auth-callback-api (Phase A)
**Found:** 2 candidates. Clean policy seam; characterizable by counting action-code invocations per statement.
**Recommended binds:** accept both if downstream uses authorizers; else defer (prose).
STOPPED for human bind. No Phase B performed.
