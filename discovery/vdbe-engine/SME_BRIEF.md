# SME brief — vdbe-engine (Phase A)
**Found:** 2 candidates. Deliberately coarse: the VM is not migratable piecemeal; SQL-level characterization recommended. Sorter (vdbesort.c) noted as a sub-seam if big-sort behaviour matters.
**Recommended binds:** accept as umbrella cards or reject in favour of SQL-level slices — SME call.
STOPPED for human bind. No Phase B performed.
