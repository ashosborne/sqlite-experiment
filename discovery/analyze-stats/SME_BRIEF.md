# SME brief — analyze-stats (Phase A)
**Found:** 2 candidates. Plan-stability seam: stats change query plans, so characterization must control stat tables.
**Recommended binds:** accept both if downstream relies on ANALYZE; else defer (prose).
STOPPED for human bind. No Phase B performed.
