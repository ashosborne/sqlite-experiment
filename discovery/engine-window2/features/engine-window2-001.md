# engine-window2-001 — General window-function engine

Confidence: observed-in-code (composed slice, run 24).
rank/dense_rank/lag/lead + framed aggregates over PARTITION BY/ORDER BY; RANGE peers, ROWS, GROUPS.
Frozen scope = the run-24 pinned cases; executed for real (pack v14).
