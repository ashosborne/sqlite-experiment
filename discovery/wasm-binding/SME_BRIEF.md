# SME brief — wasm-binding (Phase A)
**Found:** 1 umbrella candidate. UI (fiddle) deprioritized per loop rules; build tooling skipped-with-reason.
**Recommended binds:** defer unless browser delivery is in scope (prose).
STOPPED for human bind. No Phase B performed.
