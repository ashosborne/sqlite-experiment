# SME brief — triggers (Phase A)
**Found:** 2 candidates. Firing semantics (002) hides an async-like chain (trigger cascades) — flagged so humans can split cards.
**Recommended binds:** accept both if downstream schemas use triggers.
STOPPED for human bind. No Phase B performed.
