# SME brief — malloc-subsystem (Phase A)
**Found:** 2 candidates; mem0..mem5 variants recorded inside 001 (compile-time alternates, not separate slices).
**Recommended binds:** likely platform-retained; bind only if allocator behaviour matters downstream.
STOPPED for human bind. No Phase B performed.
