# engine-harvest40-008 — pragma_index_xinfo TVF

Confidence: observed-in-code (composed slice, run 50).
index_xinfo adds desc/coll/key and the trailing rowid entry (cid -1, key 0); carries the column collation.
Frozen scope = run-50 pinned cases (pack v40).
