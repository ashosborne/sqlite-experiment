# engine-harvest40-005 — json_each / json_tree full vtab columns

Confidence: observed-in-code (composed slice, run 50).
key/value/type/atom/id/parent/fullkey/path with JSONB-byte-offset ids, parent-row chains, and the optional second path argument.
Frozen scope = run-50 pinned cases (pack v40).
