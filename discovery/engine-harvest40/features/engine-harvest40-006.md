# engine-harvest40-006 — db_config leftover toggles

Confidence: observed-in-code (composed slice, run 50).
DQS_DDL, WRITABLE_SCHEMA (+DEFENSIVE enforcement), LEGACY_ALTER, RESET_DATABASE, TRUSTED_SCHEMA, load-extension gates - each with its real effect.
Frozen scope = run-50 pinned cases (pack v40).
