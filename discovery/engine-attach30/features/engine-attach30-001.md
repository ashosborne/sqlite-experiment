# engine-attach30-001 — ATTACH owns tables

Confidence: observed-in-code (composed slice, run 40).
CREATE/INSERT/SELECT/UPDATE/DELETE on attached schema.table; resolution; dup/reserved; durable.
Frozen scope = run-40 pinned cases (pack v30).
