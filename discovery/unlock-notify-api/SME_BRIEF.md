# SME brief — unlock-notify-api (Phase A)
**Found:** 1 candidate. Fire-and-forget async callback — flagged per loop rules so humans can split characterization (callback timing vs deadlock detection) at bind time.
**Recommended binds:** accept only if shared-cache mode is used downstream; else recommend defer (prose).
STOPPED for human bind. No Phase B performed.
