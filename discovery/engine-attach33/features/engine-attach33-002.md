# engine-attach33-002 — resolution and error edges

Confidence: observed-in-code (composed slice, run 43).
unqualified trigger name lives in main (cross-schema ON errors); qualified body still rejected (run-40 guard); missing/main-only body targets error no such table: aux.X at FIRE; ON resolves strictly in the trigger schema.
Frozen scope = run-43 pinned cases (pack v33).
