# SME brief — pragma-surface (Phase A)
**Found:** 2 candidates (dispatcher + vtab projection). The ~70-pragma inventory is a bind-time clustering exercise — this Phase A pass pins the seam, not each pragma.
**Recommended binds:** accept both; ask SME which pragma families the downstream estate actually sets.
STOPPED for human bind. No Phase B performed.
