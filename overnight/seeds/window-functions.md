# Seed — window-functions
SLICE_ID: window-functions
SLICE_SEED: "SQL window functions (OVER clause) — built-ins + frame execution"
SEED_ENTRYPOINTS: sqlite3WindowFunctions(), sqlite3WindowCodeStep()
OUT_OF_SCOPE: plain aggregates (builtin-scalar-agg-funcs), SELECT compilation (select-codegen)
